syntax = "proto3";

package thepack;

option java_package = "com.thepack";


service PackageService {
  rpc GetPackageNames (PackageRequest) returns (PackageResponse);
}

message PackageRequest {
  string userRole = 1;
}

message PackageResponse {
  repeated string packagesNames = 1;
}
